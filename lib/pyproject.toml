[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ufc-libs"
version = "1.0.0"
description = "Shared libraries for UFC-Predictor"
dependencies = [
    "sqlalchemy",
    "numpy",
    "xgboost"
]

[tool.setuptools.packages.find]
where = ["."]
